# Defaults to mock — no env file needed for the prototype.
# To wire up a real Moodle backend later:
#
# VITE_MOODLE_MODE=real
# VITE_MOODLE_URL=https://your-moodle.example.com
# VITE_MOODLE_TOKEN=your-webservice-token
